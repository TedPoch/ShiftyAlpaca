//package ShiftyAlpaca.router;
//
///**
// * AbstractRoutingDatasource requires information to know which actual
// * DataSource to route to. This information is typically referred
// * to as a Context. While the Context used with AbstractRoutingDatasource
// * can be any Object, an enum is used for defining them. In our example,
// * we’ll use the notion of a QueryDatabase as our context with the following implementation.
// *
// * TEST_ALPHA is currently the database used to store user queries and their results
// * TEST_BETA and on are databases in production available for targeting with EXPLAIN queries
// */
//
//public enum QueryDatabase {
//  TEST_ALPHA, TEST_BETA, TEST_GAMMA, TEST_DELTA
//}
